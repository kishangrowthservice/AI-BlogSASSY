NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
GROQ_API_KEY=your-groq-api-key
GEMINI_API_KEY=your-gemini-api-key
DEFAULT_LLM_PROVIDER=groq
ADMIN_PASSWORD=change-this-password # REQUIRED — no fallback in code, app fails to start without this
SESSION_SECRET=your-secure-session-secret
ADMIN_SESSION_TOKEN=your-admin-session-token # REQUIRED — no fallback in code, app fails to start without this
CRON_SECRET=your-cron-secret # REQUIRED — no fallback in code, app fails to start without this
